# 🪙 Projet Stakmi

Application E-commerce de vente de solutions de rangement pour numismates.
Projet pédagogique "From Scratch" (MVC PHP sans framework).

## 🛠️ Stack Technique

- **Backend** : PHP 8.2 (Vanilla MVC)
- **Base de données** : MySQL 8.0
- **Frontend** : HTML5, CSS3, Bootstrap 5, JS Vanilla
- **Outils** : WampServer, Composer

## 📂 Structure

- `/public` : Racine web (point d'entrée)
- `/app` : Cœur de l'application (Controllers, Models, Views)
- `/config` : Configuration (BDD, Stripe, etc.)

## 🚀 Installation

1. Configurer un VirtualHost pointant vers `/public`.
2. Importer la base de données (voir `/7_Scripts_BDD`).
3. Créer le fichier `.env` depuis `.env.example`.
